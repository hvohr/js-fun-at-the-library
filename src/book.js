function createTitle(title) {
  var bookIdea = "The " + title
  return bookIdea;
}

module.exports = {
  createTitle,
  // buildMainCharacter,
  // saveReview,
  // calculatePageCount,
  // writeBook,
  // editBook
}